"""Provider adapter packages.

Each immediate submodule (e.g. ``mureo.adapters.google_ads``,
``mureo.adapters.meta_ads``) wraps a single ad-platform client as a
Protocol-conformant provider for the mureo registry.
"""
