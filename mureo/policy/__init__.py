"""mureo built-in policy gates.

Unlike third-party gates (registered via the ``mureo.policy_gates``
entry-point group), the gates in this package ship *with* mureo OSS and run
by default — strategy enforcement is core mureo value, not a plugin add-on.
"""
